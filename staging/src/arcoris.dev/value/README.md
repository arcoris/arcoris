# arcoris.dev/value

## Status

Experimental staging module.

## Purpose

Small dependency-free value-state helper packages such as `maybe` and `result`.

## Source layout

The Go module root is `pkg/`. Published repositories promote `pkg/` to the
repository root.

## Testing

`cd pkg && go test ./...`
